.. _using-remsh:

Using Remsh
***********

Remsh provides a synchronous, but thread-safe, interface to invoke operations
on slaves.  The model is a dynamic collection of slave objects, with the
addition of new slaves managed by a slave listener.

.. toctree::
   :maxdepth: 2

   using/ops
